# use-web-kit Documentation Site

This is the Next.js App Router website for the `use-web-kit` library documentation.

## Development

```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000) with your browser to see the result.

The documentation is generated from `lib/hooks-data.ts`.
